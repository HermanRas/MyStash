App/Data/
# The screenshots live in a Docker named volume now; the directory is kept
# only because it is the mountpoint, and a read-only bind cannot create it.
dev/playwright/screenshots/*
!dev/playwright/screenshots/.gitkeep
App/tests/fixture.mp4
dev/sample/
